function Footer({ text }) {
  return (
    <footer className="p-5 mt-10 text-center bg-slate-200 text-sm flex justify-center align-middle">
      <p>{text}</p>
    </footer>
  );
}

export default Footer;
